#!/bin/bash

source ${PARSECDIR}/config/gcc-hooks.bldconf

CFLAGS="${CFLAGS} -O2"
CXXFLAGS="${CXXFLAGS} -O2"
LDFLAGS="${LDFLAGS}"
LIBS="${LIBS} -lhe-profiler -lhbs-pow -lenergymon-default -lrt -lstdc++"
